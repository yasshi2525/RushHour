import React, { FC, ReactNode } from "react";
interface Hoge<T> {
  children?: ReactNode;
  foo: T;
}

const Container = () => {
  const;
  return <div></div>;
};

export default () => <Container />;
