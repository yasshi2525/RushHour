# Routes Config
#
# This file defines all application routes (Higher priority routes first)
#

module:testrunner
module:pprof
# module:jobs


GET     /                                       App.Index
GET     /signout                                App.SignOut
GET     /twitter                                Auth.Twitter
GET     /twitter/callback                       Auth.TwitterCallback
GET     /google                                 Auth.Google
GET     /google/callback                        Auth.GoogleCallback
GET     /github                                 Auth.Github
GET     /github/callback                        Auth.GithubCallback
GET     /api/v1/gamemap                         ApiV1Game.Index
GET     /api/v1/players                         ApiV1Game.Players
POST    /api/v1/dept                            ApiV1Game.Departure
POST    /api/v1/extend                          ApiV1Game.Extend
POST    /api/v1/connect                         ApiV1Game.Connect
DELETE  /api/v1/rail_nodes                      ApiV1Game.RemoveRailNode

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# Catch all, this will route any request into the controller path
#
#                    **** WARNING ****
# Enabling this exposes any controller and function to the web.
# ** This is a serious security issue if used online **
#
# For rapid development uncomment the following to add new controller.action endpoints
# without having to add them to the routes table.
# *       /:controller/:action                    :controller.:action
